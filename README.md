# Multiplication Table

A simple Android application that displays a multiplication table.
It includes also Quiz and Statistics

## Build

To build the project, open it in Android Studio and run the `app` configuration.
